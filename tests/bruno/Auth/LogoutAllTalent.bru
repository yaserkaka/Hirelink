meta {
  name: LogoutAllTalent
  type: http
  seq: 5
}

post {
  url: {{base_url}}/auth/logout/all
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{talent_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
