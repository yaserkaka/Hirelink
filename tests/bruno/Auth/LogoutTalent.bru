meta {
  name: LogoutTalent
  type: http
  seq: 4
}

post {
  url: {{base_url}}/auth/logout
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{talent_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
