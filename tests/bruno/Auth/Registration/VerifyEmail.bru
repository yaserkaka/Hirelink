meta {
  name: VerifyEmail
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/verify
  body: json
  auth: inherit
}

body:json {
  {
    "verificationToken": {{verification_token}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
