meta {
  name: GetResume
  type: http
  seq: 4
}

get {
  url: {{base_url}}/talent/resume
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{talent_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
