meta {
  name: DeleteJob
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/moderation/jobs/{{job_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{moderator_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
