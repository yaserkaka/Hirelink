meta {
  name: UpdateApplicationStatus
  type: http
  seq: 8
}

patch {
  url: {{base_url}}/employer/applications/{{application_id}}
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

body:json {
  {
    "status": "HIRED"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
