meta {
  name: DeleteJob
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/employer/jobs/{{job_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
