meta {
  name: GetJob
  type: http
  seq: 5
}

get {
  url: {{base_url}}/employer/jobs/{{job_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{employer_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
