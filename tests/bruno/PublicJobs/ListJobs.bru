meta {
  name: ListJobs
  type: http
  seq: 1
}

get {
  url: {{base_url}}/jobs?mode=recent
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{talent_access_token}}
}

script:post-response {
  const firstId = res.body?.data?.[0]?.id;
  if (firstId) {
    bru.setEnvVar("job_id", firstId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
