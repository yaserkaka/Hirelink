meta {
  name: GetJob
  type: http
  seq: 2
}

get {
  url: {{base_url}}/jobs/{{job_id}}
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{talent_access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
