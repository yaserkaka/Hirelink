# development/production
NODE_ENV=development

# PostgreSQL database URL
DATABASE_URL="postgresql://dev:dev@localhost:5432/devdb?schema=public"

# PostgreSQL database URL for integration tests (Vitest E2E)
DATABASE_URL_TEST="postgresql://dev:dev@localhost:5432/devdb_test?schema=public"

# E2E tests wipe DATABASE_URL_TEST. If you point DATABASE_URL_TEST to your main DB,
# you must explicitly confirm by setting this to true.
TEST_MODE=false

# Port to run the API on
PORT=5024

# API version
API_VERSION=v1

# URL of the frontend application
FRONTEND_URL="https://hirelink.com"

# CORS allowed origins (Separate with commas)
ALLOWED_ORIGINS="https://google.com,http://localhost:5024"

# Reverse proxy trust.
# Set to true only if you deploy behind a reverse proxy like Nginx.
TRUST_PROXY=false

# Logging
# Examples: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# JWT secret keys and expiry periods
JWT_ACCESS_SECRET=
JWT_ACCESS_EXPIRY=5m
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRY=7d

# Gmail SMTP server details
GMAIL_HOST=smtp.gmail.com
GMAIL_USER=
GMAIL_PASSWORD=

# Email verification expiry period
EMAIL_VERIFICATION_EXPIRY=5m

# Moderator bootstrap credentials (moderators are not registrable via /auth/register)
MODERATOR_EMAIL=admin@hirelink.com
MODERATOR_PASSWORD=@dm!n

# Cloudinary credentials (Don't forget to enable PDF & Zip delievery)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=